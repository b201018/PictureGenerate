# PictureGenerate — Next.js scaffold

セットアップと実行（Windows PowerShell）:

```powershell
cd "c:\Users\Public\workspace\PictureGenerate"
npm install
npm run dev
```

ブラウザで http://localhost:3000 を開くと動作確認できます。

--
このリポジトリは Next.js の最小構成です。追加機能（TypeScript, ESLint, Tailwind 等）を希望する場合は教えてください。
